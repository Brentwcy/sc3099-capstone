import { AppShell } from '@/components/app-shell'
import { FeaturePlaceholder } from '@/components/feature-placeholder'

export default function CheckInPage() {
  return <AppShell><FeaturePlaceholder title="Check-in workflow" description="Active sessions, liveness prompts, location capture, and submission will be built on this route during Weeks 4–5." /></AppShell>
}
